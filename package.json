{
  "name": "coffee-time",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "postinstall": "husky install",
    "start:app": "yarn workspace @coffee-time/app start",
    "build:app": "yarn workspace @coffee-time/app build",
    "test:app": "yarn workspace @coffee-time/app test"
  },
  "devDependencies": {
    "@commitlint/cli": "^12.1.4",
    "@commitlint/config-angular": "^12.1.4",
    "husky": "^6.0.0",
    "lint-staged": "^11.0.0",
    "prettier": "^2.3.0"
  },
  "workspaces": [
    "packages/app"
  ],
  "lint-staged": {
    "**/*.{js,jsx,ts,tsx,json,md}": [
      "prettier --write"
    ]
  }
}
